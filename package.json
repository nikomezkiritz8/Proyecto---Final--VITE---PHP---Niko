{
  "name": "ejercicio-vite---0",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node scripts/dev.mjs",
    "build": "node scripts/clean-dist.mjs && vite build",
    "preview": "npm run build && node scripts/preview.mjs"
  },
  "devDependencies": {
    "sass": "^1.100.0",
    "vite": "^8.0.12"
  }
}
